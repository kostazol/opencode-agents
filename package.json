{
  "name": "opencode-agents",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && npm run check:tools && node --test tests-ts/*.test.mjs",
    "check:tools": "tsc -p tsconfig.tools.json",
    "test:all": "npm test && python3 -m unittest discover -s tests -p 'test*.py' && bash tests/test-cli.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
